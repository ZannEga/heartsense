Put your two exported model files right in this folder, with these exact names:

  mlp_model.tflite
  tabnet_model.tflite

(Or change the filenames/paths in lib/services/tflite_service.dart if you'd
rather use different names.)

Delete this .txt file once your real .tflite files are here - it's just a
placeholder so Git/zip tools keep the empty folder.
